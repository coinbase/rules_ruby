Gem::Specification.new do |s|
  s.name                     = "{name}"
  s.summary                  = "{name}"
  s.authors                  = {authors}
  s.version                  = "{version}"
  s.licenses                 = {licenses}
  s.files                    = {dests}
  s.require_paths            = {require_paths}

  {gem_runtime_dependencies}
end
